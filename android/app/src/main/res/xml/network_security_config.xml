<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration
    Security: CRITICAL (9.1) - Comprehensive Network Security
    
    ADHCC Compliance: Network Security + Certificate Pinning
    
    This configuration enforces:
    1. HTTPS-only communication (cleartext disabled globally)
    2. SSL Certificate Pinning for production domains
    3. User-installed CA certificates BLOCKED
    4. No debug overrides in production
    
    Compliance: OWASP MASVS-NETWORK-1, MASVS-NETWORK-2, PCI-DSS 4.1-4.2, HIPAA 164.312(e)
    
    Certificate pins generated: January 2026
    Pins must be regenerated when SSL certificates are renewed.
-->
<network-security-config>
    
    <!-- Base configuration - HTTPS-only globally, NO user certs -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Production Configuration - HTTPS-Only, Certificate Pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">247tech.net</domain>
        <domain includeSubdomains="true">api.247tech.net</domain>
        
        <!-- Trust only system certificates -->
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        
        <!--
            CERTIFICATE PINNING - ACTION REQUIRED BEFORE PRODUCTION
            =========================================================
            These pins are PLACEHOLDER values. They MUST be replaced with
            real SHA-256 SPKI hashes once production SSL certificates are
            deployed to 247tech.net / api.247tech.net.

            To generate real pins, run:
                ./scripts/generate-cert-pins.sh 247tech.net

            Then replace the <pin> values below with the output.
            See docs/CERTIFICATE_PINNING_GUIDE.md for full instructions.
        -->
        <pin-set expiration="2027-01-21">
            <!-- TODO: Replace with real leaf certificate pin after SSL deployment -->
            <pin digest="SHA-256">PLACEHOLDER_LEAF_PIN_REPLACE_BEFORE_PRODUCTION=</pin>
            <!-- TODO: Replace with real intermediate CA pin after SSL deployment -->
            <pin digest="SHA-256">PLACEHOLDER_INTERMEDIATE_PIN_REPLACE_BEFORE_PRODUCTION=</pin>
        </pin-set>
    </domain-config>
    
</network-security-config>
